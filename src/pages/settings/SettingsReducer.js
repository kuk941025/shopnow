
const initState = {
    loaded: false 
}

const SettingsReducer = (state = initState, action) => {
    switch (action.type){
        default: 
        return state;
    }
}

export default SettingsReducer;
